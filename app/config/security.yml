# To get started with security, check out the documentation:
# https://symfony.com/doc/current/security.html
security:

    # https://symfony.com/doc/current/security.html#b-configuring-how-users-are-loaded
    providers:
        in_memory:
            memory: ~

        steamauth:
            id: steam.user_provider

    firewalls:
        # disables authentication for assets and the profiler, adapt it according to your needs
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        main:
            anonymous: ~
            # activate different ways to authenticate

            # https://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticate
            #http_basic: ~

            # https://symfony.com/doc/current/security/form_login_setup.html
            #form_login: ~

            provider: steamauth
            steam:
                default_route: index
            remember_me:
                secret: '%secret%'

            logout:
                path: /logout
                target: /

    access_control:
        - { path: ^/config, roles: ROLE_EDIT_CONFIG }
        - { path: ^/news/add, roles: ROLE_NEWS_MANAGE }
        - { path: ^/news/delete, roles: ROLE_NEWS_MANAGE }
        - { path: ^/vidya-in-2017, roles: IS_AUTHENTICATED_ANONYMOUSLY, methods: [GET] }
        - { path: ^/vidya-in-2017, roles: ROLE_ADD_VIDEO_GAME, methods: [POST] }
        - { path: ^/crew, roles: ROLE_PROFILE_VIEW }
        - { path: ^/crew/new, roles: ROLE_PROFILE_ADD_USER }
        - { path: ^/crew/search, roles: ROLE_PROFILE_ADD_USER }
        - { path: ^/crew/.+/edit, roles: ROLE_PROFILE_EDIT_DETAILS }
        - { path: ^/awards, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/awards/manage, roles: ROLE_AWARDS_FEEDBACK, methods: [GET] }
        - { path: ^/awards/manage, roles: ROLE_AWARDS_EDIT, methods: [POST] }
        - { path: ^/nominees, roles: ROLE_NOMINATIONS_VIEW, methods: [GET] }
        - { path: ^/nominees, roles: ROLE_NOMINATIONS_EDIT, methods: [POST] }
        - { path: ^/vote, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/vote/code, roles: ROLE_VOTING_VIEW }
        - { path: ^/winners, roles: IS_AUTHENTICATED_ANONYMOUSLY, methods: [GET] }
        - { path: ^/winners, roles: ROLE_AWARDS_EDIT, methods: [POST] }
        - { path: ^/results, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/referrers, roles: ROLE_REFERRERS_VIEW }
        - { path: ^/countdown, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/stream, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/finished, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/credits, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/videos, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/soundtrack, roles: IS_AUTHENTICATED_ANONYMOUSLY }


