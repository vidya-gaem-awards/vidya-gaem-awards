{% extends 'base/master.html.twig' %}

{% block fullTitle '2023 Vidya Gaem Awards' %}

{% block head %}
  <style type="text/css">
    @font-face {
        font-family: 'ChronoType';
        src: url('/2023images/fonts/ChronoType.woff') format('woff');
        font-weight: normal;
        font-style: normal;
    }

    body {
      background: black;
      font-family: 'ChronoType', monospace;
      color: white;
      font-weight: 300;
    }

    a {
      color: white;
    }

    a:hover {
      color: whitesmoke;
    }

    @keyframes title {
      0% {
        opacity: 0;
      }
      100% {
        opacity: 1;
      }
    }

    @keyframes appear {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes award {
      0% {
        opacity: 0;
        transform: translateX(10px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    #title {
      opacity: 0;
      animation: {{ fastAnimations ? '2s' : '2s' }} ease-out forwards title;
      font-family: "ChronoType", sans-serif;
    }

    #awardList .award {
      opacity: 0;
    }

    .animate-in {
      opacity: 0;
      animation: {{ fastAnimations ? '1s' : '2s' }} ease-out {{ fastAnimations ? '0ms' : '6s' }} forwards appear;
    }

    .animate-in-delay {
      opacity: 0;
      animation: {{ fastAnimations ? '1s' : '2s' }} ease-out {{ fastAnimations ? '0s': '7s' }} forwards appear;
    }
  </style>

  <script type="text/javascript">
    $(document).ready(function () {
        $('.award').each(function(index){
            $(this).css({
                'animation': '1s ease-out ' + (index * {{ fastAnimations ? 0 : 75 }} + {{ fastAnimations ? 1000 : 2000 }}) + 'ms forwards award'
            });
        });
    });
  </script>
{% endblock %}

{% block css %}
  <link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Mansalva&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sedgwick+Ave&family=Sedgwick+Ave+Display&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,400i" rel="stylesheet">
{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-12" style="text-align: center;">

        <img src="{{ asset('2023images/logo.png') }}" style="width: 600px; margin: 0 auto; max-width: 100%;" class="mb-4">
        <img src="{{ asset('2023images/logo-wide.png') }}" style="width: 800px; margin: 0 auto; margin-top: 50px; max-width: 100%;" class="mb-4">

        <div style="font-size: 80px; line-height: 80px; margin-top: 60px;" id="title">
          <div>
            The 2023 /v/GAs
          </div>
          <div style="font-size: 50px;" class="animate-in">
            Nominations opening Dec. 8th
          </div>
        </div>
        <div style="font-size: 30px;" class="animate-in">
          <a href="http://discord.gg/4e8JQB4">http://discord.gg/4e8JQB4</a>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
