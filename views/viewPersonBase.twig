{% extends 'master.twig' %}

{% block content %}
<div class="page-header">
    <h1>
        <img src="data:image/png;base64,{{ _user.avatar }}" style='height: 40px; margin-bottom: 5px;' />
        {{ _user.name }}
        <small>{{ _user.primaryRole }}</small>
    </h1>
    <p>Back to <a href="{{ path('people') }}">Cast and Crew</a></p>
</div>

<div class="row">
    {% block subContent %}{% endblock %}
</div>
{% endblock %}
